## 注释

在 Nix 语言中，用 `#` 表示注释，在它之后直到行末的部分都会被忽略。

## 空格、缩进、换行

空格、缩进、换行只是为了方便读者阅读，无实际意义。

以下写法等价：

```nix
# 写法1
let
 x = 1;
 y = 2;
in x + y

# 写法2
let x=1;y=2;in x+y
```

## 名称和值

使用 `=` 将名称分配给值，形成 `名称-值` 对。

```nix
# 将名称 foo 分配给值 123
foo = 123
```