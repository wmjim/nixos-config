`inherit` 用于将已有作用域中某个名称的值赋给嵌套作用域中的同名变量。

其目的是为了方便，避免多次重复相同的名称。

```nix
let
  x = 1;
  y = 2;
in
{
  inherit x y; # 等价于 x = x; y = y;
}
```

- 直接从同名变量获取值，这被称为 **继承**

也可以使用 `()` 从特定的属性集中`继承`名称：

```nix
let
  a = { x = 1; y = 2; };
in
{
  inherit (a) x y; # 等价于 x = a.x; y = a.y;
}
```